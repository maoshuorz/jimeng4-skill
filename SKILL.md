---
name: jimeng4
description: 即梦 4.0 Pro 文生图 - 火山引擎即梦 AI 图片生成 API 封装。用于根据文本提示词生成高质量图片，支持多种尺寸和异步任务处理。
---

# 即梦 4.0 Pro 文生图

火山引擎即梦 AI 图片生成 4.0 API 封装，支持文本到图片生成。

## 功能特性

- 🎨 **文生图**：根据提示词生成高质量图片
- 🖼️ **多种尺寸**：支持 1024x1024, 1280x720, 720x1280 等
- ⚡ **异步任务**：支持任务提交和结果查询
- 🔄 **自动重试**：内置超时和重试机制

## 前置要求

```bash
pip3 install volcengine
```

## API 密钥配置

需要火山引擎即梦 AI 服务权限：

1. 登录 https://console.volcengine.com/
2. 开通 "即梦 AI 图片生成 4.0"
3. 获取 Access Key (AK) 和 Secret Key (SK)

## 使用方法

### Python API

```python
from scripts.jimeng4 import Jimeng

client = Jimeng(ak='你的AccessKey', sk='你的SecretKey')

# 生成图片
result = client.generate('一只可爱的猫咪')
print(result['images'])  # 图片 URL 列表
```

### 命令行使用

```bash
python scripts/jimeng4.py <ak> <sk> "提示词"
```

## 参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| prompt | string | 必填 | 提示词描述 |
| width | int | 1024 | 图片宽度 |
| height | int | 1024 | 图片高度 |
| max_wait | int | 60 | 最大等待时间（秒）|

## 返回值

```python
{
    'task_id': '任务ID',
    'status': 'done',
    'images': ['https://...'],  # 图片URL列表
    'binary_data': ['base64...']  # Base64编码图片
}
```

## 示例

```python
# 生成方形图片
result = client.generate('一只在草地上玩耍的金毛犬')

# 生成横屏图片
result = client.generate('日落时分的海滩', width=1280, height=720)

# 生成竖屏图片
result = client.generate('城市夜景', width=720, height=1280)
```

## 注意事项

- 图片生成需要时间，默认最多等待 60 秒
- API 调用会消耗火山引擎账户余额
- 确保 AK/SK 有即梦 AI 服务权限

## License

MIT
